<p id="notice"><%= notice %></p>

<p>
  <strong>Project:</strong>
  <%= Project.find_by_id(@task.project_id).name %>
</p>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Completed:</strong>
  <%= @task.completed %>
</p>

<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>
